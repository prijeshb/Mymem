"""Interop layer — protocol surfaces that let other agents reach MyMem (ADR-017).

`mcp/` exposes the wiki over the Model Context Protocol with OKF-formatted payloads.
A future `a2a/` sibling will add the agent-to-agent coordination layer (Phase 3).
"""
