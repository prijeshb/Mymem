# Screenshots — MCP interop

Drop screenshots here; they're embedded by `../mcp-interop-connected.md`.

Expected files:
- `claude-desktop-mymem-connected.png` — Claude Desktop showing the `mymem` server + tools connected.
- `claude-cli-mymem-connected.png` — (optional) `claude mcp list` / a tool call in the terminal.

The agent can't screen-capture the GUI apps, so these are added manually.
